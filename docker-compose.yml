version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: tron-mysql
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: "admin"
      MYSQL_DATABASE: "tron"
    volumes:
      - tron_db_data:/var/lib/mysql

volumes:
  tron_db_data: